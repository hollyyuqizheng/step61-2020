package com.google.sps.data;

import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

@RunWith(JUnit4.class)
public final class MaximizeScheduledTimeSchedulerTest {

  // /** Tests that the reverse order comparator sorts tasks by duration in descending order. */
  // @Test
  // public void testTaskComparator() {
  //   Task task1 = new Task("Task 1", "First task", SchedulerTestUtil.DURATION_30_MINUTES,
  // SchedulerTestUtil.PRIORITY_ONE);
  //   Task task2 = new Task("Task 2", "Second task", SchedulerTestUtil.DURATION_15_MINUTES,
  // SchedulerTestUtil.PRIORITY_ONE);
  //   Task task3 = new Task("Task 3", "Third task", SchedulerTestUtil.DURATION_20_MINUTES,
  // SchedulerTestUtil.PRIORITY_ONE);
  //   Task task4 = new Task("Task 4", "Fourth task", SchedulerTestUtil.DURATION_45_MINUTES,
  // SchedulerTestUtil.PRIORITY_ONE);
  //   List<Task> tasks = Arrays.asList(task1, task2, task3, task4);

  //   Collections.sort(tasks,
  // Collections.reverseOrder(LongestTaskFirstScheduler.sortByTaskDurationAscendingThenName));

  //   List<Task> expected = Arrays.asList(task4, task1, task3, task2);
  //   Assert.assertEquals(expected, tasks);
  // }

}
